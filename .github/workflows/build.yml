name: 构建和发布Qt学生管理系统

# 触发条件
on:
  push:
    branches: [ main ]      # 推送到main分支时触发
    tags:
      - 'v*'                # 推送版本标签时触发（如 v1.0.0）
  pull_request:
    branches: [ main ]      # 创建PR到main分支时触发
  workflow_dispatch:        # 允许在GitHub Actions页面手动触发

jobs:
  # Windows构建任务
  build-windows:
    name: 构建Windows版本
    runs-on: windows-latest
    
    # 设置权限：允许创建和更新Releases
    permissions:
      contents: write        # 允许创建Release和上传文件
      
    steps:
    # 步骤1: 检出代码
    - name: 📥 检出代码
      uses: actions/checkout@v4
      with:
        submodules: 'recursive'  # 自动初始化和更新子模块
      
    # 步骤2: 设置MSVC编译环境
    - name: 🔧 设置MSVC
      uses: microsoft/setup-msbuild@v2
      
    # 步骤3: 安装Qt开发环境
    - name: 🔧 安装Qt 6.6.3
      uses: jurplel/install-qt-action@v4
      with:
        version: '6.6.3'           # Qt版本（ElaWidgetTools推荐6.6.3以获得最佳兼容性）
        host: 'windows'            # 主机平台
        target: 'desktop'          # 目标平台
        arch: 'win64_msvc2019_64'  # 架构：MSVC 2019 64位
        cache: true                # 启用缓存以加快构建速度
        
    # 步骤4: 配置CMake项目
    - name: ⚙️ 配置CMake
      run: |
        mkdir build
        cd build
        cmake .. -DCMAKE_BUILD_TYPE=Release
        
    # 步骤5: 编译项目
    - name: 🔨 编译项目
      run: |
        cd build
        cmake --build . --config Release
        
    # 步骤6: 使用windeployqt部署Qt依赖
    - name: 📦 部署Qt依赖
      run: |
        cd build\Release
        windeployqt StudentSystem.exe
        
        # 复制 ElaWidgetTools 库文件
        echo "正在复制 ElaWidgetTools 库文件..."
        if (Test-Path "..\ElaWidgetTools\ElaWidgetTools\ElaWidgetTools.dll") {
          Copy-Item -Path "..\ElaWidgetTools\ElaWidgetTools\ElaWidgetTools.dll" -Destination "." -Force
          Write-Host "✅ ElaWidgetTools.dll 复制成功"
        } else {
          Write-Host "⚠️ 警告: ElaWidgetTools.dll 未找到，尝试其他路径..."
          if (Test-Path "..\ElaWidgetTools\ElaWidgetTools\Release\ElaWidgetTools.dll") {
            Copy-Item -Path "..\ElaWidgetTools\ElaWidgetTools\Release\ElaWidgetTools.dll" -Destination "." -Force
            Write-Host "✅ ElaWidgetTools.dll 从Release目录复制成功"
          }
        }
        
    # 步骤7: 创建发布包
    - name: 📂 打包发布文件
      run: |
        # 创建发布目录
        mkdir StudentSystem-Windows
        
        # 复制主程序
        Copy-Item -Path "build\Release\StudentSystem.exe" -Destination "StudentSystem-Windows\"
        
        # 复制所有DLL文件
        Copy-Item -Path "build\Release\*.dll" -Destination "StudentSystem-Windows\" -ErrorAction SilentlyContinue
        
        # 复制Qt插件目录
        Copy-Item -Path "build\Release\platforms" -Destination "StudentSystem-Windows\platforms" -Recurse -ErrorAction SilentlyContinue
        Copy-Item -Path "build\Release\styles" -Destination "StudentSystem-Windows\styles" -Recurse -ErrorAction SilentlyContinue
        Copy-Item -Path "build\Release\imageformats" -Destination "StudentSystem-Windows\imageformats" -Recurse -ErrorAction SilentlyContinue
        Copy-Item -Path "build\Release\iconengines" -Destination "StudentSystem-Windows\iconengines" -Recurse -ErrorAction SilentlyContinue
        Copy-Item -Path "build\Release\generic" -Destination "StudentSystem-Windows\generic" -Recurse -ErrorAction SilentlyContinue
        Copy-Item -Path "build\Release\networkinformation" -Destination "StudentSystem-Windows\networkinformation" -Recurse -ErrorAction SilentlyContinue
        Copy-Item -Path "build\Release\tls" -Destination "StudentSystem-Windows\tls" -Recurse -ErrorAction SilentlyContinue
        
        # 复制翻译文件
        Copy-Item -Path "build\Release\translations" -Destination "StudentSystem-Windows\translations" -Recurse -ErrorAction SilentlyContinue
        
        # 复制说明文档
        Copy-Item -Path "README.md" -Destination "StudentSystem-Windows\"
        Copy-Item -Path "DEPLOYMENT.md" -Destination "StudentSystem-Windows\" -ErrorAction SilentlyContinue
        Copy-Item -Path "ELAWIDGETTOOLS_INTEGRATION.md" -Destination "StudentSystem-Windows\" -ErrorAction SilentlyContinue
        
        # 压缩成ZIP文件
        Compress-Archive -Path "StudentSystem-Windows\*" -DestinationPath "StudentSystem-Windows.zip"
        
    # 步骤8: 上传构建产物到GitHub Actions
    - name: ⬆️ 上传构建产物
      uses: actions/upload-artifact@v4
      with:
        name: StudentSystem-Windows
        path: StudentSystem-Windows.zip
        retention-days: 30          # 保留30天
        
    # 步骤9: 自动发布最新版本（每次推送main分支时）
    - name: 🚀 发布最新开发版本
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        name: 最新开发版 (自动构建)
        tag_name: latest           # 使用"latest"标签
        files: StudentSystem-Windows.zip
        draft: false               # 不创建草稿
        prerelease: true           # 标记为预发布版本
        body: |
          ## 🚀 自动构建版本 - Fluent-UI 现代化界面
          
          ✨ **新特性**：现已集成 ElaWidgetTools Fluent-UI 组件库！
          
          这是最新的自动构建版本，每次代码更新都会自动发布。
          
          **✨ UI升级亮点**：
          - 🎨 全新 Microsoft Fluent Design 风格
          - 🌫️ Windows 11 Acrylic 亚克力模糊效果
          - 📦 3D 卡片设计，精致阴影和圆角
          - ✨ 流畅的动画和过渡效果
          - 💬 现代化非侵入式消息提示
          - 🖼️ 无边框现代化窗口
          - 🎭 支持亮色/暗色主题切换
          
          **构建信息**：
          - 📅 构建时间：${{ github.event.head_commit.timestamp }}
          - 🔨 提交信息：${{ github.event.head_commit.message }}
          - 👤 提交者：${{ github.event.head_commit.author.name }}
          - 🔗 提交SHA：${{ github.sha }}
          - 🎨 UI框架：ElaWidgetTools (Fluent-UI)
          - 🛠️ Qt版本：6.6.3
          
          **下载说明**：
          1. 下载 `StudentSystem-Windows.zip`
          2. 解压到任意目录
          3. 双击 `StudentSystem.exe` 运行
          4. 查看 `ELAWIDGETTOOLS_INTEGRATION.md` 了解新界面特性
          
          **系统要求**：
          - Windows 10/11 (64位)
          - 推荐 Windows 11 以获得完整亚克力效果
          - 无需安装Qt环境
          - 无需额外依赖
          
          **注意**：这是开发版本，可能包含未经充分测试的新功能。
          如需稳定版本，请下载正式发布的版本（v1.0.0等）。
          
          ---
          
          📖 [使用文档](https://github.com/SenyFish/StudentSystem/blob/main/README.md) | 
          🎨 [UI集成说明](https://github.com/SenyFish/StudentSystem/blob/main/ELAWIDGETTOOLS_INTEGRATION.md) | 
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
    # 步骤10: 创建正式发布版本（仅在推送tag时执行）
    - name: 🎉 发布正式版本
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v1
      with:
        files: StudentSystem-Windows.zip
        draft: false                # 不创建草稿
        prerelease: false           # 不标记为预发布版本
        generate_release_notes: true # 自动生成发布说明
        body: |
          ## 🎉 正式发布版本 - Fluent-UI 现代化界面
          
          ✨ **全新界面**：采用 Microsoft Fluent Design 设计语言！
          
          这是经过测试的稳定版本，推荐生产环境使用。
          
          **✨ 主要特性**：
          - 🎨 现代化 Fluent-UI 风格界面
          - 🌫️ Acrylic 亚克力玻璃效果（Windows 11）
          - 📦 精美 3D 卡片设计
          - 📊 学生信息增删改查功能
          - 🔍 智能搜索和排序
          - ✨ 流畅的动画效果
          - 💬 友好的用户提示
          - 🎭 支持主题切换
          
          **下载说明**：
          1. 下载下方的 `StudentSystem-Windows.zip`
          2. 解压到任意目录
          3. 双击 `StudentSystem.exe` 运行
          4. 查看 `ELAWIDGETTOOLS_INTEGRATION.md` 了解UI特性
          
          **系统要求**：
          - Windows 10/11 (64位)
          - 推荐 Windows 11 以获得完整亚克力效果
          - 无需安装Qt环境
          - 无需额外依赖
          
          **技术栈**：
          - Qt 6.6.3
          - ElaWidgetTools (Fluent-UI)
          - C++17
          
          ---
          
          📖 [完整文档](https://github.com/SenyFish/StudentSystem) | 
          🎨 [UI集成说明](https://github.com/SenyFish/StudentSystem/blob/main/ELAWIDGETTOOLS_INTEGRATION.md) | 
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

